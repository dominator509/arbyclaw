# Runtime opportunity-trace dedup recovery coverage (2026-06-13)

- `crates/arb-core/src/runtime.rs` now has `validate_runtime_opportunity_trace_recovery_for_corpus(...)`, allowing runtime-level opportunity trace recovery summary tests against caller-supplied local historical-fixture corpora instead of only the built-in Phase 27 fixture set.
- Added `runtime_trace_recovery_from_duplicate_candidate_corpus_preserves_deduplicated_counts`, proving runtime restart-recovery summary accounting preserves deduplicated planner-handoff replay counts for a duplicate-candidate corpus with `discovered_candidates = 1`, `audit_trace_records_replayed = 1`, `recovered_trace_checkpoints = 1`, and `missing_trace_checkpoints = 0`.
- This strengthens the local evidence path behind GAP-0054 by showing runtime-level opportunity trace replay/recovery accounting is not limited to the default fixture corpus and remains coherent after candidate-id deduplication.
- `PRODUCTION_GAP_TRACKER.md` was reconciled so GAP-0054 no longer asks for already-covered local runtime lifecycle state-checkpoint or durable planner-handoff replay tests; the remaining future validation is now phrased in terms of live/provider-backed ingestion, external/deployment scenario corpora, sandbox/live calibration, deployment-host opportunity trace replay under orchestration, and broader external historical backtest replay.
- Full validation after the code/doc updates passed on 2026-06-13: `python3 scripts/generate_structure_manifest.py`, `python3 scripts/validate_structure.py`, `python3 -m py_compile scripts/validate_structure.py scripts/generate_structure_manifest.py scripts/validate_deployment_host_runtime.py scripts/validate_arm_cross_check.py`, `cargo fmt --check`, `cargo check --workspace`, `cargo test -p arb-core` (405 passed), `cargo test --workspace` (462 passed), and `cargo clippy --workspace --all-targets -- -D warnings`.