# Deployment runtime aggregate preflight enforcement (2026-06-13)

- Extended `scripts/validate_deployment_host_runtime.py` so nested runtime-smoke reports expose a structured `production_runtime_preflight` object with status, local smoke flags, blocker count, deployment-host evidence booleans, and production-ready flag.
- Extended `scripts/validate_deployment_runtime_gate.py` to enforce that embedded Phase 49 preflight contract: `validation_passed == true`, status `BlockedPendingProductionHostValidation`, both local smoke validations true, positive unresolved blocker count, service-manager and disk-full evidence availability false, and production-ready false.
- Updated `ROADMAP.md`, `PHASE_28_SUBROADMAP.md`, `PHASE_49_SUBROADMAP.md`, and `PRODUCTION_GAP_TRACKER.md` so Phase 28/49/runtime-lifecycle wording reflects that the aggregate deployment gate now explicitly enforces the local preflight boundary rather than only carrying it through runtime-smoke output.
- Validation after the change passed: `rtk python3 scripts/validate_deployment_host_runtime.py --run-runtime-smoke --runtime-smoke-iterations 1 --config config.example.toml --runtime-workspace <fresh-dir> --json`, `rtk python3 scripts/validate_deployment_runtime_gate.py --json`, `rtk python3 -m py_compile scripts/validate_deployment_host_runtime.py scripts/validate_deployment_runtime_gate.py`, `rtk python3 scripts/validate_structure.py`, `rtk cargo fmt --check`, `rtk cargo check --workspace`, `rtk cargo test --workspace` (495 passed), and `rtk cargo clippy --workspace --all-targets -- -D warnings`.