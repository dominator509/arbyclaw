# Phase 18 handoff-candidate aggregate gate (2026-06-13)

- Added `scripts/validate_agentic_handoff_candidate_gate.py` as a bounded local aggregate validator for the Phase 18 handoff candidate surface.
- The gate composes three existing local-only validators:
  - `cargo run -p arb-agent -- validate-agentic-handoff-audit --workspace <fresh-dir>`
  - `python3 scripts/validate_hardening_core_gate.py --json [--require-systemd-analyze]`
  - `python3 scripts/validate_deployment_evidence_checklist.py --json`
- It emits schema `arbyclaw.agentic_handoff_candidate_gate.v1` and asserts:
  - handoff audit reports a non-empty package id, positive artifact/unresolved-gap/live-funds-blocker/replayed-record counts, audit/state fail-closed behavior, and false external-agent/external-validation/production/live-funds/public-exposure/secret flags
  - nested hardening-core gate passes with unsafe fields all false
  - deployment evidence checklist bundle passes with all unsafe fields false
- Wired CI to run `python3 scripts/validate_agentic_handoff_candidate_gate.py --json --require-systemd-analyze` in place of the previous standalone hardening-core step, and removed the earlier redundant standalone `validate-agentic-handoff-audit` CI step because the new aggregate already enforces it.
- Updated `scripts/validate_structure.py`, `STRUCTURE_MANIFEST.md`, `ROADMAP.md`, `PHASE_18_SUBROADMAP.md`, `HANDOFF_CONTEXT.md`, `ARCHITECTURE.md`, and `PRODUCTION_GAP_TRACKER.md` so the repo now describes the Phase 18 local handoff-candidate aggregate gate accurately without claiming any external-agent execution or production readiness.
- Validation after the change passed:
  - `rtk python3 scripts/validate_agentic_handoff_candidate_gate.py --json`
  - `rtk python3 -m py_compile scripts/validate_agentic_handoff_candidate_gate.py scripts/validate_hardening_core_gate.py scripts/validate_deployment_evidence_checklist.py`
  - `rtk python3 scripts/generate_structure_manifest.py`
  - `rtk python3 scripts/validate_structure.py`
  - `rtk cargo fmt --check`
  - `rtk cargo check --workspace`
  - `rtk cargo test --workspace` (495 passed)
  - `rtk cargo clippy --workspace --all-targets -- -D warnings`
- Also probed the local `C:\Scripts\Start-DeepSeekClaude.ps1` path because GAP-0072 mentions external agent runtimes. The launcher exists, but a read-only smoke attempt failed with `401 Authentication Fails, Your api key ... is invalid`, so local external-agent execution still cannot be claimed from the current machine state and remains an external/authenticated blocker.